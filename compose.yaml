# This file is being replaced by docker-compose.yml
# Please use docker-compose.yml instead
version: "3.8"

services:
  photoprism:
    image: ${DOCKER_IMAGE_NAME:-photoprism2}:${DOCKER_IMAGE_TAG:-latest}
    # This is a placeholder - please see docker-compose.yml for the full configuration
    profiles: ["do-not-use"]
